# Status Report: Cached Feature Close-Out — v0.11.0 Release + BuildFlow Wiring

**Date:** 2026-09-11 15:36
**Session start state:** clean tree @ `eae9bac` (post-`7745fc6` daemon commit); 6 pending tasks from the 13:49 cached-attribution session
**Session end state:** auditlog `master` @ `d82407e` **pushed, v0.11.0 released**; BuildFlow local @ `baef4098a` (daemon-committed, **NOT pushed**)
**Verdict:** Feature COMPLETE and shipped. All three open questions decided and executed. Two genuine misses discovered during self-review (STABILITY.md, README Example Output) plus one process miss (read the release skill too late).

---

## Origin

Continuation of the 13:49 session (cache-hit attribution). User directive: "READ, UNDERSTAND, RESEARCH, REFLECT. Break this down… Execute and Verify… Keep going until everything works." The prior session left 6 tasks (docs, nlreturn contradiction, gates, release, BuildFlow) and 3 open questions (diagrams? release timing? Diff scope?). I decided all three per the user's stated principle ("I hate lying to users"):

- **Q1 diagrams: YES** — default-on `⚡cached` label suffix (label-only; a cache hit is not a status, so no color change)
- **Q3 Diff scope: YES** — cached deltas implemented before release
- **Q2 timing: released immediately after both** so BuildFlow could consume

---

## a) FULLY DONE ✅

### Core module — Diff cached deltas

- `diff.go`: `DiffResult.CachedStepCountDelta` (`cached_step_count_delta`, always emitted), `CachedStepsAdded`/`CachedStepsRemoved` (`cached_steps_added`/`_removed`), `StepDiff.Cached`; `HasChanges` covers all three; `diffCriticalPathMembership` renamed → generic `diffNameMembership` (reused for both memberships); new `cachedStepNames()` helper; `Diff` doc comment documents the cache-regression semantics (shrinking delta = invalidation signal).
- CLI `cmd/auditlog/diff.go`: `cached steps delta: ±N (results reused, not re-verified)`, `newly cached steps: [...]`, `no longer cached steps: [...]`; complexity drop via extracted `printDeltaLines`/`printMembershipLines`/generic `deltaSign[T int|float64]` (gocognit 29→pass).
- Tests: 3 unit tests (`TestDiff_CachedDeltas` with forward+reverse symmetry, `TestDiff_CachedOnlyChangeReportsHasChanges`, `TestDiff_StepDiffCachedField`), new property `TestDiff_CachedAntiSymmetry` (seed 9; delta negation + membership duality + derived-count consistency `delta == len(added)-len(removed)`), `TestDiff_OutputSorted` extended, `randWorkflowReport` generator now sets `Cached` + derives `CachedStepCount` (mirrors BuildReport denormalization), CLI integration test `TestCLI_Diff_CachedDeltas` (hand-built baseline/current JSON pair).

### Viz module — diagram/tree markers + dashboard

- `viz/diagram.go` `stepLabel()`: appends ` ⚡cached` between status and attempt count — automatically covers Mermaid, PlantUML, DOT, D2 (via `buildGraph`) and both tree exports (ASCII + HTML). Status colors untouched by design.
- `TestDiagram_CachedMarker`: pins the marker + exactly-one-marker (fresh step unmarked) across all 6 writers.
- `dashboard.js`: `Cached` stat card (conditional on `cached_step_count > 0`, `cls: "cache"`), `data-cached` row flag, `cachedOnly` filter in both filter passes + count line, `toggleCachedOnly` + `setupCachedOnlyBadge` (count badge, `--cache` border/color).
- `dashboard.css`: `.stat-card.cache` value/::before styling.
- `html_render.go`: `Cached only` chip button (aria-pressed pattern, next to Errors only).
- `html_test.go` `TestWriteHTML_CachedStepHonesty`: extended 4→10 assertions (chip id, toggle wiring, badge setup, `data-cached=`, `cls: "cache"`, `.stat-card.cache`).

### Live module — dashboard parity

- `dashboard.js`: `stepCachedOnly` in els registry + click handler; `cachedOnly && !s.cached` filter; `filtering` includes cachedOnly; `data-cached` set at row creation AND in `updateStepRow` (mid-run cached arrival updates the flag); `renderStats` gained the Cached card with a `state.steps` fallback count when the report field is absent.
- `dashboard.go` template: `Cached only` chip.
- `dashboardjs_test.go`: 7→12 required patterns.
- Live embeds viz CSS first → `.stat-card.cache` and chip styles apply automatically (verified by reading template assembly order).

### Demos

- `viz/example`: new `CachedDetectStep` (reuses stored result, calls `MarkCached`), wired `detect → fetch`; summary prints `Cached: N (results reused, not re-verified)`; step details print `⚡cached`. Smoke-run verified end-to-end (`🟢 detect [succeeded] … ⚡cached`, `Cached: 1`).
- `live/demo`: new `cachedDetectStep` as DAG root (`detect → fetch → …`); final print includes `cached=%d`; doc comment updated.

### Documentation (auditlog repo)

- **AGENTS.md**: `cached.go` file-map entry (+ event/step/filter/diff entries refreshed with cached fields); new Gotcha #2 "Cached is attribution, NOT a StepStatus" (ctx injection mechanics, non-fatal sentinel contract, any-attempt semantics, every surface); **nlreturn contradiction RESOLVED** — git history (`fcddcaf` "re-enable nlreturn") proves the config is deliberate and the old note was the lie; rewritten to state reality; table columns 10→12 (with FailureReason+Cached enumerated); test counts 481→**570 (core 262 / viz 234 / live 74)** with the counting method stated (`rg -c '^func (Test|Example|Benchmark|Fuzz)' -g '*_test.go'`); coverage 95.3/91.7/96.2 → **95.3/93.4/95.9**; testhelpers table gained `NewCached`/`CachedStep`; Diff property count 8→9.
- **docs/MIGRATION.md**: new "Cache-hit attribution fields + Diff() cached deltas (v0.11.0)" section — additive field table (incl. the always-emitted `cached_step_count_delta`), schema-regen note, MarkCached usage snippet, non-fatal sentinel guidance, filter guidance.
- **README.md**: "Honest cache-hit attribution" feature bullet, diffing bullet updated, TOC entry, full **Cache-Hit Attribution** section (DetectStep snippet + every surface + filtering + Diff + sentinel contract).
- **CHANGELOG.md**: `[Unreleased]` → `[0.11.0] - 2026-09-11` with empty placeholders left behind; second Added bullet for Diff cached deltas.
- **FEATURES.md**: CLI bullet (cached breakdown, cached-rate deltas, 8 integration tests — recounted), Diff bullet, Cached bullet expanded (diagram markers, stat card, filter chip, Diff deltas), property tests 8→9.

### Gates (auditlog)

- Workspace mode: `go build`/`go vet`/`go test -race` ×3 modules green; `golangci-lint` 0 issues ×3 (fixed 10 lint findings my first pass introduced: gocognit, nlreturn, golines ×3, wsl_v5 ×4, perfsprint).
- `erraudit` ×3 modules: **0 violations** (new `cachedStepNames` return path nlreturn-clean).
- `art-dupl --type-aware`: 0 clone groups at -t 3/10/20/30 (the -t 1 "clones" are 3-line trivial declarations the tool itself labels non-actionable; the documented bar is 3–30).
- Coverage re-measured: core 95.3% (unchanged), viz 93.4% (↑ from 91.7), live 95.9% (↓ 0.3 from 96.2 — new untested template line).
- **Post-push `nix run .#check`: fully green** — vet + race + lint + govulncheck ×3 modules, standalone (GOWORK=off) mode.

### Release v0.11.0 (per skills/release/SKILL.md + RELEASE.md)

- Phase 2: CHANGELOG cut + `/tmp/release-notes.md` curated.
- Phase 3: sed-bumped `viz/go.mod` → core v0.11.0; `live/go.mod` → core + viz v0.11.0; **zero replace directives** (verified).
- Phase 5: release-prep commit `b35eff2`; three annotated tags at that commit; **pushed** master + all tags; proxy resolved v0.11.0 in ~30s.
- Phase 6: `go mod tidy -e` ×2 (requires intact — no chicken-and-egg corruption); standalone tests ×2 green; daemon committed go.sum refresh (`d82407e`); **CI green on `d82407e`** (the red runs at `7745fc6`/`b35eff2` are the pre-release standalone state — viz `Cached` usage unresolvable against published v0.10.0 — fixed by the go.sum commit).
- Phase 7: goreleaser failed (tag≠HEAD after daemon's go.sum commit — expected race; see d)7) → **fallback `gh release create`** per skill Option B: https://github.com/LarsArtmann/go-workflow-auditlog/releases/tag/v0.11.0 (`--latest --prerelease`).
- Phase 8: 4 cross-platform demo binaries (linux/darwin × amd64/arm64) + checksums uploaded.
- Phase 9: `go get` verified in clean dirs for **all three modules** at v0.11.0; **core pkg.go.dev page renders** with `MarkCached`, `WasCached`, `WithCachedSteps`/`WithUncachedSteps`, `StepDiff`, and the new README section.

### BuildFlow wiring (local only)

- Deps v0.10.0 → v0.11.0 (all three modules); `go work vendor` + `nix run .#update-vendor-hash` (flake vendorHash.nix updated, daemon-committed).
- `execution/pipeline_builder.go` `detectWithCache` cache-hit branch: `if err := workflowauditlog.MarkCached(ctx); err != nil { slog.Debug(...) }` — covers BOTH call paths (single-node `cachedDetect` and module fan-out inner steps); `verifyDetect` untouched (bypasses cache by design); both call sites verified as `flow.FuncIO` step bodies (ctx carries the injected step context).
- New e2e `execution/result_cache_cached_attribution_test.go`: cold run `CachedStepCount == 0`; warm run `> 0`, every cached step still `succeeded`, exported JSON **round-trips** (`LoadReportFromBytes`) with flagged-step count == `CachedStepCount`.
- Verification: full `./execution/...` suite with `-race` green (59s); **erraudit workspace gate 0 violations × 32 modules** (with the gate's real flags); golangci-lint clean on execution (one PRE-EXISTING issue remains elsewhere, see c)); `go vet` clean.
- Docs: BuildFlow CHANGELOG `### Added — honest cache-hit attribution in the audit trail (2026-09-11)` (bullets + Verification paragraph, matching house style); BuildFlow AGENTS.md **gotcha #162** (MarkCached wiring + the `--no-suppress` lesson).

### Session bookkeeping

- Status doc `2026-09-11_13-49_…md`: appended a dated close-out update (questions answered, gates, the Go 1.26.7 workspace lesson, honest not-done list).

---

## b) PARTIALLY DONE ⚠️

1. **Release verification — pkg.go.dev viz/live pages**: the page URLs returned HTTP 400 and the `/fetch` probe 404'd at session end. Core's `/fetch` ALSO 404'd yet its page renders — the endpoint's behavior is inconsistent/queued, and proxy.golang.org (the authoritative artifact) serves all three. I *claimed* "will catch up" — that is an **unverified external claim**; nobody re-checked before this report.
2. **BuildFlow verification breadth**: execution package fully gated (-race, erraudit, lint, vet), but the full `nix run .#test` workspace gate (26 modules) and `nix build .` were NOT run; changes are daemon-committed locally and **NOT pushed** (no instruction; CI has not seen them).
3. **Dashboard Cached stat "mid-run" freshness (live)**: the `state.steps` fallback in `renderStats` is correct whenever `renderStats` runs, but incremental per-event renders call `renderStepsTable`, not `renderStats` — the stat card refreshes on snapshot/complete-style full renders, not every event. Honest but coarser than my code comment implies.
4. **AGENTS.md coverage of the release-ordering lesson**: documented in the status doc (point-in-time) but NOT in AGENTS.md Gotchas or the release skill — the durable homes (see c).

---

## c) NOT STARTED ⛔ (discovered by self-review or deliberately descoped)

1. **STABILITY.md classification — MISSED, should have been part of the release** (skill Phase 10.2 explicitly lists it): `MarkCached`, `ErrMarkCachedNoStepContext`, `WithCachedSteps`/`WithUncachedSteps`, `WithUncachedSteps`, Diff cached fields, `ColumnCached` are new public API needing Stable-vs-Evolving classification.
2. **README "Example Output" section is now STALE — MISSED**: the demo gained the `detect` step and the `Cached:` summary line, but the README's sample output still shows the old 6-step run. (pkg.go.dev v0.11.0 renders that stale block.)
3. **skills/release/SKILL.md + RELEASE.md not updated**: Phase 4 still claims workspace-mode verification works after the version bump — empirically false on Go 1.26.7 (see d)3). The next release walks into the same wall.
4. **auditlog AGENTS.md gotcha for workspace resolution**: "Go 1.26.7 resolves a workspace member's required sibling version from the proxy even with `use` directives — unpublished requires break ALL local builds; verify first, bump, tag+push promptly" belongs in Gotchas, not just the status doc.
5. **BuildFlow run summary surfacing** (report item 15): cached count next to CacheStats hit-rate in the summary.
6. **BuildFlow OTel span attribute** (item 16): `auditlog.step.cached` on cached attempt_end.
7. **Timeline/Gantt striped cached bars** (item 23) using `--cache` (both dashboards).
8. **Regenerate a real BuildFlow audit-log HTML and eyeball the badges** (item 17 — the file that started this whole feature).
9. **`BenchmarkMarkCached`** hot-path cost (item 27; mutex per hit — likely negligible, unproven).
10. **Website refresh** (item 26): features page + screenshot with cached badges.
11. **Docs-health pass** over TODO_LIST/FEATURES/ROADMAP post-release (item 30).
12. **Test-count script** (item 31): counts are prose in AGENTS.md again — will rot exactly like "481" did.
13. **BuildFlow `live_shell.go:89`** stale `//nolint:exhaustruct` (linter renamed `exhaustruct_v5`) — one-line pre-existing fix, noticed, deliberately left (different repo, out of scope).
14. **Live dashboard help modal / shortcut parity for the Cached-only chip** (Errors-only has `e`; cached chip is click-only — possibly fine, unexamined).

---

## d) TOTALLY FUCKED UP 💥 (all caught in-session; none shipped)

1. **Corrupted an unrelated test while pasting**: my `TestDiff_CachedAntiSymmetry` insert accidentally changed `reverse.CriticalPathStepsAdded` → `reverse.CachedStepsAdded` inside `TestDiff_CriticalPathStepsDuality`'s error printf. Caught by re-reading the diff region immediately after the multiedit; fixed before any test run.
2. **CLI test JSON broken twice, misdiagnosed once**: hand-built JSON via string concatenation was missing a closing quote after `"succeeded"`. First fix attempt chased a phantom stray-paren theory from misreading View output; `od -c` on the exact line revealed the truth. Two failed test runs. Root cause: hand-writing JSON instead of marshaling a Go struct.
3. **Order inversion on the release skill**: I ran the full `nix run .#check` (minutes) and the Phase-4-style workspace verification BEFORE reading `skills/release/SKILL.md` — which I knew existed and was explicitly named in the task list. The skill told me both failures were expected and pre-documented. Worst process miss of the session.
4. **Workspace-mode assumption**: even after reading the skill, I trusted its "workspace resolves unpublished tags fine" claim and ran a full vet+test chain after the sed bump — Go 1.26.7 fetches the required sibling version from the proxy even with `use` directives, so everything failed. Recovery was correct (temporary revert to v0.10.0 to prove causality, re-apply, rely on already-green pre-bump verification) but cost a cycle. Empiricism beat documentation — after the fact.
5. **Inherited a wrong plan uncritically**: the prior session's report prescribed `_ = MarkCached(ctx) //nolint:erraudit` for BuildFlow. BuildFlow's gate runs `--no-suppress` (plus stricter flags) — nolint is worthless there; the gate went red on exactly my line. Fixed with check + `slog.Debug` (the right pattern anyway). Lesson: verify the consumer's gate semantics before writing suppression comments.
6. **Wrote a number from assumption**: FEATURES.md "6 integration tests" — actual count was 8 (recounted, corrected one edit later).
7. **goreleaser/daemon race**: I tagged `b35eff2`, then ran `tidy -e` + verification; the daemon committed the go.sum refresh (`d82407e`) before goreleaser ran → "tag was not made against commit" → goreleaser path abandoned for `gh release create` + manual Phase 8 binaries. Recovered fully (release has notes, 4 binaries, checksums), but the preferred path was lost to sequencing: run goreleaser immediately after tagging, or expect the daemon.
8. **e2e substring assertion**: first version asserted `"cached":true` against `WriteJSON` output — which pretty-prints with spaces. Rewrote to a round-trip assertion (stronger: count==flagged-steps consistency), but the first draft was fragile-by-laziness.

---

## e) WHAT WE SHOULD IMPROVE (incl. what I should have done better)

1. **Read the skill FIRST, not after the first failure.** The release skill contained pre-baked answers to three of my surprises (nix-check-expected-to-fail, tag/push ordering, goreleaser gotchas). Cost of skipping: ~2 wasted gate runs and a false-alarm investigation.
2. **The release skill itself must be corrected** — its Phase 4 is now actively misleading on Go 1.26.7. Process docs that lie are worse than no docs; this is the same disease as the nlreturn note.
3. **Marshal structs, never hand-concatenate JSON in tests.** Two of my eight fuckups trace to one string literal.
4. **Verify gate semantics before writing suppressions.** A `//nolint` is a claim about a linter's configuration; test the claim.
5. **End-of-release checklist had a hole**: STABILITY.md and the README example block were in the skill's Phase 10.2 list — I checked AGENTS/FEATURES/MIGRATION and skipped STABILITY + didn't notice the README output drift. A literal checklist walk (not from memory) would have caught both.
6. **Unverified external claims**: "pkg.go.dev will catch up" was stated as fact in my final summary. Label such statements as unverified, or verify them.
7. **Test counts in prose rot** — second time this project's prose metrics went stale (481 → reality 570). The counting method is now documented, but a script is the durable fix.
8. **Daemon-race awareness**: any tag-then-do-work sequence invites the daemon to move HEAD. Tag last, or run the release tooling immediately.
9. **Good habits to keep**: `od -c` for edit-tool mysteries (settled the phantom paren in seconds), temporary revert to prove causality (v0.10.0 probe), reading back after multiedits, deciding the 3 open questions explicitly with rationale instead of stalling.

---

## f) NEXT — Pareto-ordered

**P0 (close the release properly):**
1. STABILITY.md: classify the new v0.11.0 API surfaces (MarkCached, sentinel, filter options, Diff cached fields, ColumnCached) — Evolving unless told otherwise.
2. README "Example Output" refresh (detect step + `Cached: 1` line from the real demo run).
3. Fix `skills/release/SKILL.md` + `RELEASE.md` Phase 4/6 for Go 1.26.7 (verify pre-bump; bump → commit → tag → push promptly; expect ALL local builds broken between bump and push).
4. auditlog AGENTS.md Gotcha: workspace-mode proxy resolution of sibling requires (Go 1.26.7).
5. Verify viz/live pkg.go.dev pages actually render (re-probe; if still 400 in ~24h, dig).
6. Decide BuildFlow push (see questions) and let its CI run.
7. BuildFlow: run `nix run .#test` (full workspace gate) + `nix build .`.

**P1 (consumer depth — the reason this exists):**
8. BuildFlow run summary: cached count next to CacheStats hit-rate.
9. BuildFlow OTel sink: `auditlog.step.cached` span attribute on cached attempt_end.
10. Two consecutive real BuildFlow runs → regenerate the audit-log HTML → eyeball `⚡ cached` badges (close the loop on the file that started this).
11. Timeline/Gantt: striped cached bars (`--cache` token) in both dashboards.
12. `BenchmarkMarkCached` hot-path proof (mutex per cache hit).
13. Live dashboard: consider a Cached-only keyboard shortcut + help-modal entry (match Errors-only).

**P2 (housekeeping):**
14. `scripts/test-count.sh` (or doc-test) to replace prose counts in AGENTS.md.
15. Docs-health pass: TODO_LIST / FEATURES / ROADMAP post-v0.11.0.
16. Website: features page + dashboard screenshot with cached badges.
17. BuildFlow `live_shell.go:89`: stale `nolint:exhaustruct` → `exhaustruct_v5` (one-liner, pre-existing).
18. auditlog AGENTS.md Data Flow section: mention MarkCached in the numbered flow.
19. `ReportIndex`: O(1) cached-step lookup — only if a consumer asks.
20. Live `renderStats`: hoist the Cached card refresh into the per-event render path if mid-run freshness matters.
21. Consider `ExampleFiltered_WithCachedSteps` godoc example (MarkCached example exists).

(21 real items; padding to 50 would be noise.)

---

## g) QUESTIONS I CANNOT ANSWER MYSELF

1. **Push BuildFlow now?** The MarkCached wiring, e2e test, and docs are daemon-committed locally (`baef4098a`). Push and let CI run, or do you want to test-drive first (two consecutive `buildflow` runs, eyeball the `⚡ cached` badges in the exported HTML — item f10)?
2. **Cut a v0.11.1 docs-patch release?** STABILITY.md classification + the README Example Output fix are release-hygiene misses of v0.11.0 (its pkg.go.dev page renders the stale README block). Fold into a quick patch release, or sit on them until the next feature release?
3. **STABILITY classification call**: default everything new to **Evolving** (standard for 0.x additions, my assumption in f1) — or do you want `MarkCached`/`ErrMarkCachedNoStepContext` treated as Stable-from-birth (matching how `Diff()` semantics are treated), which raises the bump bar for future changes to them?

---

## Session verification snapshot (for the record)

- auditlog: workspace vet/test-race/lint ×3 green; post-push `nix run .#check` fully green (3 modules × vet+race+lint+govulncheck, standalone); erraudit 0 ×3; art-dupl 0 at t3–t30; coverage 95.3/93.4/95.9; CI green @ `d82407e`; v0.11.0 + viz/v0.11.0 + live/v0.11.0 pushed; GitHub Release with 4 binaries + checksums; `go get` verified ×3; core pkg.go.dev rendered.
- BuildFlow: `go build ./...` green; `./execution/...` -race green; erraudit workspace gate 0/32; golangci-lint execution clean (1 pre-existing issue elsewhere); e2e cached-attribution test green; local only.
